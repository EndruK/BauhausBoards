$('.sidebar').on('click', '.btnEditorStrokeSize', openPopup);

function openPopup(event) {
  /*var button = $(this);
  var parent = button.parent();
  var swiper = $('.sidebarSwiper');

  console.log(pos);
  //parent.append("<div class='popupStrokeSize'>");
  var popup = $('<div class="popupStrokeSize">').appendTo(parent);

  console.log(popup.height());
  var top = button.position().top - (popup.height()/2) + (button.height()/2);
  var left = swiper.position().left + swiper.width() + 20;

  popup.css('left',left + 'px');
  popup.css('top' ,top + 'px');*/
}